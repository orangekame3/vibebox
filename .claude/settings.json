{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r fp; if [ -n \"$fp\" ] && echo \"$fp\" | grep -qE '(\\.env$|\\.env\\.|credentials|secret|password)'; then echo 'Protected file pattern detected' >&2; exit 2; fi; exit 0; }",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
